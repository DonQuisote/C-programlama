#include <stdio.h> 
\\include içermek demek
 int main ()
  {
     printf("Hello C");
     return(0); 
  }

\\include ile kütüphane tanımlıyor
  " <<studio.h> " ana fonksiyonların olduğu
     Kütüphane 
       int main () yapısı ana fonksiyonu
 tanımlıyor
       bunu f(x) g(x) fonksiyonu gibi 
düşünebilirsin
       f(g(x)) şeklinde bir fonksiyon 
tanımlaman gerekirse kullanicaksin 
yoksa içi boş int main() olucak
 fonksiyonu tanımladıktan sonra {} 
parantezi ile fonksiyonun içeriğini
 giriyoruz.
     return  geri dön demek 
     re tekrar turn dönmek
     
    
